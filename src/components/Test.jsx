import React from 'react';
import PropTypes from 'prop-types';

export class Test extends React.Component {
  // constructor(props) {
  //   super(props)
  // }

  render() {
    // const { } = this.props;

    return (
      <div>
      </div>
    )
  }
}

Test.propTypes = {
  // mapStateToProps
  // mapDispatchToProps
  // ownProps
};
